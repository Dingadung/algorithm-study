class Rectangle:
    def __init__(self, side, angle, width, height):
        self.side = side
        self.angle = angle
        self.width = width
        self.height = height


p1 = Rectangle(4, 4, 10, 11)

print(p1.side)
print(p1.angle)
print(p1.width)
print(p1.height)
