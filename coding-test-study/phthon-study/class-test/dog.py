class Dog:
    def __init__(self, name, type, gender):
        self.name = name
        self.type = type
        self.gender = gender


p1 = Dog("John", "리트리버", "male")

print(p1.name)
print(p1.type)
print(p1.gender)
